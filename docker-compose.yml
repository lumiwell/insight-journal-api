services:
  db:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: lumi
      POSTGRES_PASSWORD: 123456
      POSTGRES_DB: journal
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
